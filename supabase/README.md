# Local Supabase

This directory is initialized in Phase 0 so migrations have a home.  
Auth, schema, and RLS land in Phase 4. Do not treat this config as production-ready.

Use the Supabase CLI when that work starts:

```bash
npx supabase start
npx supabase migration new <name>
```
