# Minimal local config. Expand with `supabase init` / CLI defaults in Phase 4
# if you need the full generated file.

project_id = "keypath-local"

[api]
enabled = true
port = 54321
schemas = ["public"]

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000"]
